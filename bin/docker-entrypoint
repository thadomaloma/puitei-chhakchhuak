#!/bin/bash -e

# Validate production configuration before accepting traffic. Migrations belong
# to Railway's pre-deploy command, not every web container.
if [ "${@: -2:1}" == "./bin/rails" ] && [ "${@: -1:1}" == "server" ]; then
  ./bin/rails production:check_environment
fi

exec "${@}"
