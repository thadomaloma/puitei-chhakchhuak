<% content_for :title, t("staff.edit_title", name: @staff.name) %><%= render "shared/back_link", path: staff_path(@staff), label: @staff.name %><div class="mx-auto mt-4 max-w-3xl"><%= render "shared/page_header", eyebrow: @staff.employee_code, title: t("staff.edit"), description: t("staff.edit_description") %><div class="mt-5"><%= render "form", staff_member: @staff %></div></div>
