test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

# Railway Buckets are private S3-compatible object storage. Railway can inject
# these standard AWS variables from the bucket into the application service.
railway:
  service: S3
  access_key_id: <%= ENV["AWS_ACCESS_KEY_ID"] %>
  secret_access_key: <%= ENV["AWS_SECRET_ACCESS_KEY"] %>
  region: <%= ENV.fetch("AWS_DEFAULT_REGION", "auto") %>
  bucket: <%= ENV["AWS_S3_BUCKET_NAME"] %>
  endpoint: <%= ENV["AWS_ENDPOINT_URL"] %>
  force_path_style: <%= ENV.fetch("AWS_S3_URL_STYLE", "virtual") == "path" %>

# Use bin/rails credentials:edit to set the AWS secrets (as aws:access_key_id|secret_access_key)
# amazon:
#   service: S3
#   access_key_id: <%= Rails.application.credentials.dig(:aws, :access_key_id) %>
#   secret_access_key: <%= Rails.application.credentials.dig(:aws, :secret_access_key) %>
#   region: us-east-1
#   bucket: your_own_bucket-<%= Rails.env %>

# mirror:
#   service: Mirror
#   primary: local
#   mirrors: [ amazon, google, microsoft ]
